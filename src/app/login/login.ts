export interface Login
{
    register_firstname:string;
    register_lastname:string;
    register_number:number;
    register_email:string;
    register_password:string;
    register_repeatpassword:string;
    
}